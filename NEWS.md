# packup 0.1.0

* Initial release of the `packup` package.
* Implemented a greedy weight distribution algorithm in C using a binary min-heap.
* Added R wrapper function `assign_items()` with strict input validation.
* Set up automated documentation using `roxygen2` and GitHub Actions deployment for `pkgdown` site.